fn main() {
    // This is single line comment
    let a = [1, 2, 3, 4, 5];

    /*
     * This is multiline comment
     */
    let first = a[0];
    println!("first = {}", first);

    let second = a[1];
    println!("second = {}", second);
}
